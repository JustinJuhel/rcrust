use crate::axis::raw_axes::RawAxes;

pub struct Filter {}

impl Filter {
    /// TODO: impl & doc
    pub fn process(axes: RawAxes) -> RawAxes {
        axes
    }

    /// TODO: impl & doc
    pub fn smooth(axes: RawAxes) -> RawAxes {
        axes
    }
}
